[build]
  base    = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

# SPA redirect — all routes serve index.html for client-side routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
